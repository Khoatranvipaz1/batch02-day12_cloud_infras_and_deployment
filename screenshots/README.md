# Deployment Screenshots

1. `dashboard.png`: Railway web service and Redis are online.
2. `running.png`: deployment is active and successful.
3. `testing1.png`: public `/health` and `/ready` results.
4. `testing2.png`: authenticated `/ask` result and rate-limit `429`.

Do not include API keys, Redis passwords, or other secrets in screenshots.
