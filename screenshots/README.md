# Required Screenshots

Add these files after cloud deployment:

1. `dashboard.png`: Railway or Render service dashboard.
2. `running.png`: deployment status and public domain.
3. `test.png`: successful `/health`, `/ready`, and authenticated `/ask` calls.

Do not include API keys, Redis passwords, or other secrets in screenshots.
